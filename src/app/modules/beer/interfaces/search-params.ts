/**
 * Interface used in mat-select ont the search page and beers service.
 * Value: parameter for obtaining beers.
 * Name: name of the search parameter visible to the user (in the dropdown, for example).
 */
export interface SearchParams {
    value: string,
    name: string
}
